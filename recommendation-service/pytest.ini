[pytest]
pythonpath = src
testpaths = test
asyncio_mode = auto
markers =
    unit: isolated unit tests
    integration: tests requiring PostgreSQL/Testcontainers infrastructure
addopts = --strict-markers
